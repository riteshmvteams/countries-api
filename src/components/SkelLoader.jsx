export default function SkelLoader({ height, width }) {
  return (
    <div
      style={{
        height,
        width,
        borderRadius: "0.75rem",
      }}
      className="skelLoader"
    ></div>
  );
}
